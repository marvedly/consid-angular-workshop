import { FormatArrayWithDotsPipe } from './format-array-with-dots.pipe';

describe('FormatArrayWithDotsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatArrayWithDotsPipe();
    expect(pipe).toBeTruthy();
  });
});
